# Personal Workflow Agent

Starter scaffold from `SPEC.md` for a 2-person hackathon build.

Work split:
- `extension/` = browser capture, popup, launcher, library view
- `backend/`, `ai/`, `runtime/`, `fixtures/` = APIs, storage, generation path, tool runtime, demo data

Start here:
- `docs/PARALLEL_START.md`
- `docs/CONTRACTS.md`
- `SPEC.md`
