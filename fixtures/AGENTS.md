# Platform AI Rules

- Touch `backend/**`, `ai/**`, `runtime/**`, and `fixtures/**` only.
- Do not edit `extension/**`.
- Keep fixtures aligned with `docs/CONTRACTS.md`.
- If fixture format changes affect another lane, update the shared contract docs too.
- Prefer small, demo-friendly samples over giant datasets.
