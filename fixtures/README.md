# Demo Fixtures

Use small samples that unblock:
- extension integration
- tool runtime checks
- demo rehearsal
- cached fallback paths

Start with Domain A. Expand only after the full path works once.
