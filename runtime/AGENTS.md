# Platform AI Rules

- Touch `backend/**`, `ai/**`, `runtime/**`, and `fixtures/**` only.
- Do not edit `extension/**`.
- Keep generated tool output aligned with `docs/CONTRACTS.md`.
- If the runtime contract changes, update `docs/CONTRACTS.md` and `docs/CONTRACTS_CHANGELOG.md` together.
- Keep artifacts single-file and browser-runnable.
